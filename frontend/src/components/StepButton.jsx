import { RaisedButton, FlatButton } from 'material-ui'
import NavigatorNext from 'material-ui/svg-icons/image/navigate-next'
import NavigatorBefore from 'material-ui/svg-icons/image/navigate-before'

export const NextStepButton = props => (
    <RaisedButton
        {...props}
    />
)

export const BackStepButton = props => (
    <FlatButton
        {...props}
    />
)

export const FinishButton = props => (
    <RaisedButton
        {...props}
    />
)
